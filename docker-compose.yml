services:
 api:
  build: ./api
  ports:
  - "3000:3000"
  volumes:
  environment:
    POSTGRES_USER:
    POSTGRES_PASSWORD:
    POSTGRES_DB:
    POSTGRES_HOST: store

  store:
    build:
    ports: 
     - "2506:2506"
    environment:
      POSTGRES_USER:
      POSTGRES_PASSWORD:
      POSTGRES_DB:
